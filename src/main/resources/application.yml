spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/doan_langthang
    username: root
    password: illusion
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 3MB


cloud:
  aws:
    bucket:
      name: langthang-user-photos
    credentials:
      access-key: AKIAUNGRYFYGATBT7VAZ
      secret-key: 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
    region:
      static: ap-southeast-1
    stack:
      auto: false
    public:
      base-url: https://${cloud.aws.bucket.name}.s3-${cloud.aws.region.static}.amazonaws.com

application:
  image:
    support-type: image/png,image/jpg,image/jpeg